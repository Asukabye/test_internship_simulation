# 流程

## 澄清会

### 澄清会疑问清单(2026-08-24)

1. **取消订单后库存是否回滚**: 订单取消后, 已占用的库存是否释放/回滚? 若回滚, 回滚时机是取消成功时还是支付超时时? (v1.0 未在文档中明确, 以接口实际行为为准) 
2. **库存不足时下单的返回**: 库存不足时下单失败, 应返回什么错误码/提示文案? 是否区分"部分不足"与"全部不足"? (v1.0 统一按 400 业务错误处理) 
3. **秒杀限购规则**: 秒杀是否限制每人购买数量? 限购数量是多少? 同一用户能否重复抢购同一秒杀商品? (v1.0 未做限购, 简化处理)

### 澄清会结论(2026-08-24)

| 编号                | 结论                                                         | 状态     |
| ------------------- | ------------------------------------------------------------ | -------- |
| #1 取消订单库存回滚 | 业务上应释放已占用库存(否则库存只出不进); v1.0 无支付超时自动取消机制, 若回滚时机即"取消成功时"。**实现是否已回滚: DEV 核对中, 确认后答复并更新本表** | ⏳ 待确认 |
| #2 库存不足返回     | **400 + code=`INSUFFICIENT_STOCK`**, 文案「商品『商品名』库存不足」(带商品名)。**不区分**"部分不足/全部不足": 整单校验, 任一商品不足即整单失败(400), 不会部分成功, 购物车条目不变 | ✅ 已确认 |
| #3 秒杀限购         | **v1.0 不限购**: 同一用户可重复抢购同一秒杀商品直至库存耗尽; 单次数量由请求体 quantity 决定(≥1), 无每人上限。限购规则(每人 N 件/场次)留待后续版本由运营配置 | ✅ 已确认 |

### 澄清会结论(2026-08-25)

F1 答复：取消订单后库存是否回滚

**业务结论（方向性，可定）**：应该回滚。理由上轮说过——库存只出不进，系统跑几天所有商品都会缺货，这在业务上说不通。回滚时机就是**取消成功时**（v1.0 没有支付超时自动取消，不存在另一个时机）。

**实现核对结果（说实话，有点含糊）**：我翻了取消订单的代码路径，状态流转（待支付 → 已取消）和取消时间记录是有的，但**库存释放、库存流水恢复这部分，我在代码里没看到明确的处理逻辑**。这块交付前不是我亲手写的，我不太想拍胸脯说"做了"或"没做"——你们实测最直接：

> 建议验证步骤（你们的测试设计，我只说系统事实）：下单一个含商品 X 的订单 → 记下商品 X 当前库存和 `inventory_ledger` 流水 → 取消该订单 → 再查商品 X 库存和流水。

如果取消后库存**没有**恢复、流水也没有对应的入库记录，那就是实现遗漏，请按 bug 单流程提交（复现步骤 + 预期/实际），我确认后走排期修复；如果恢复了，说明我看漏了，我以你们实测结果为准，并把结论更新进文档。

这个结论我已经同步到需求文档 §6 澄清会结论表（#1 标记"待 QA 实测确认"），你们测完把结果发我，无论哪种情况我都会回一个明确结论。

另外提醒一句：这正好和你们后面要做的库存类用例（扣减、对账、并发）强相关，建议 F1 的实测放在库存测试那批一起做，效率高。

### 澄清会疑问清单(2026-08-25)

取消订单接口的限制条件与需求文档有差异，接口文档未举出剩余订单状态被尝试取消订单接口请求时的返回内容。
需求文档：

```
            支付回调                   模拟发货              确认收货
  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
  │  待支付       │──▶│  已支付       │──▶│  已发货       │──▶│  已完成       │
  │ PENDING_     │   │ PAID         │   │ SHIPPED      │   │ COMPLETED    │
  │ PAYMENT      │   └──────────────┘   └──────────────┘   └──────────────┘
  └──────┬───────┘
         │ 取消
         ▼
  ┌──────────────┐
  │  已取消       │
  │ CANCELLED    │
  └──────────────┘
```

接口文档：
❌ 失败案例(HTTP 400, 已完成订单不可取消):

### 澄清会结论(2026-08-25)

文档已补全（接口文档 3.12 新增"各订单状态的取消返回"表，5 种状态全部列明，差异处标注"已登记缺陷单，修复后文档同步更新"）。

**处理方案（按流程走）：**

1. **请正式提单**：按 Bug 单格式提交（编号、复现步骤、预期/实际、级别、证据——你们可以拿 alice 的种子订单直接复现：订单 2 是已支付，订单 3 是已发货，对它们调取消接口都会返回 200 而非预期的 400）。我们登记缺陷台账。
2. **修复计划**：状态机校验（仅待支付可取消）和澄清会 #1 的取消库存回滚是同一套逻辑，我建议**一起排期改**，避免同一段代码改两遍——所以这次不单独热修，v1.0 冻结期不动代码，排后续版本统一处理。如果你们觉得级别够高必须马上修，在 Bug 单里标出来，我们内部再评估。
3. **重复取消的说明**：已取消订单再取消返回 200（幂等），状态机里没定义这条边，当前实现按幂等处理，不影响数据正确性。你们如果认为应该返回错误，也可以一并提单，我们讨论后定规则。

**测试建议**（你们的用例设计，我只说事实）：状态机相关的断言别只测取消一个接口——发货（仅已支付可发货）、确认收货（仅已发货可确认收货）这两个的校验是完整的，可以作为"正确实现"的对照组；取消接口的差异按上面表格实测记录即可。

缺陷登记后我会给回复编号，方便你们跟踪。还有其他状态机场景的疑问一起提。

## 用例设计(2026-08-25——08-26)

产物:E-shop 单接口功能测试用例

## 用例评审(2026-08-26)

### 用例评审

148 条用例全部读完了，我从"需求/接口文档/实现"三个维度逐条核对，问题不少，其中**一个阻塞级问题会让全部用例连不上服务**。先看关键证据再给完整评审意见。

先说评审结论：**不通过，需修订后复审**。主要问题清单如下（详细版我已生成评审意见文档）：

🔴 阻塞级（不改全部用例无法执行）

**B-1 端口错误：所有用例的 Base URL 写成了 8001~8015**（注册 8001、登录 8002、商品 8003……秒杀 8015），而服务实际只监听 **8000**。除了健康检查，其余 140+ 条用例全部会连接失败。基准地址应为 `http://127.0.0.1:8000`，这个你们 JMeter 的 HTTP 请求默认值里改一次即可，但用例表里的 URL 列也要同步。

🟠 严重级（预期结果与实现/文档不符，执行必挂）

| 用例                                | 问题                                                         | 正确预期                  |
| ----------------------------------- | ------------------------------------------------------------ | ------------------------- |
| E-shop_036/037/038/039、084/085/086 | 参数名写成了 `pagesize`，实际参数名是 **`page_size`**；未知查询参数会被静默忽略，用例预期全落空（比如 036 预期 422，实际会 200 返回 10 条） | 改参数名后重算预期        |
| E-shop_042/045                      | 预期"keyword/category 不带引号 → 422"——URL 查询参数本质都是字符串，不存在类型校验，实际会 200（total=0 或空） | 200 + 空列表              |
| E-shop_087                          | 预期 status=非法值 → 422；实现**不校验枚举**，返回 200 + 空列表 | 200 + items 空            |
| E-shop_144                          | 库存不足预期 PARAM_ERROR；实际是 **400 `INSUFFICIENT_STOCK`**（澄清会 #2 已确认） | INSUFFICIENT_STOCK        |
| E-shop_121                          | 已发货订单确认收货是**正常流程**（SHIPPED→COMPLETED），用例预期"失败"，逻辑反了 | 200 成功                  |
| E-shop_120~123                      | 确认收货失败文案全部复制成"仅已支付订单可发货"，实际是**"仅已发货订单可确认收货"** | 修正文案                  |
| E-shop_062/065/066/068/069/070 等   | 路径参数直接写占位符 `{item_id}`/`{order_id}` 没给具体值，请求会 404；且"路径参数为空"类用例预期 422，实际是**404（路由不存在）** | 填实际 ID；空路径预期 404 |
| E-shop_064                          | 预期条目不存在返回 PARAM_ERROR；实际 **404 `NOT_FOUND`"购物车条目不存在"** | NOT_FOUND                 |

🟡 一般级（数据/逻辑/笔误）

- **E-shop_072/073/074 vs 075 前置矛盾**：072~074 标题"清空购物车，创建成功"，但购物车为空时下单是 CART_EMPTY 失败（075 自己写了）。`cart_item_ids=null/[]/省略` 的语义是**结算全部**，不是清空；必须写明前置"购物车有 N 条数据"；
- **E-shop_078 库存不足用例缺前置**：保温杯（库存 0）不在 alice 购物车里，直接下单会先撞 PARAM_ERROR（条目无效）。需先加购保温杯再下单；
- **E-shop_137**：标题"订单 id 不存在"但请求体 `order_id: 1`（是存在的），应为 999999 之类；**E-shop_141**：标题"非秒杀商品"但传的 product_id=11 正是秒杀商品，应传普通商品（如 3）；
- **E-shop_058**：9.9×999=**9890.1**，预期 9891.1 算错了；
- **E-shop_018**：请求体 nickname 是空串（合法值，会注册成功），与标题"不为字符串"矛盾；
- **E-shop_026**：'Alice' 大小写用例——当前库排序规则不区分大小写，'Alice' 很可能**登录成功**，与预期"失败"不符（需求里没定义用户名大小写策略，这条建议标注"待确认"）；
- **E-shop_002/008/022**：预期响应笔误（test_1 写成 dave、11 字符写成 ABC、nickname 预期写成字面量 "username"）；
- **E-shop_049**：alice 购物车现在有 3 条种子数据（昨天刚补的），预期"items 为空"不成立，用 bob/carol 或改预期；
- **数据依赖**：种子订单 1/2/3 被支付、取消、发货、详情多个用例共享，谁先执行都会改掉订单状态，导致后续用例前提失效；秒杀库存只有 5 件，138/145 等用例会耗尽库存。建议每个用例自造数据（加购→下单），或明确执行顺序、必要时重置库；
- **E-shop_083**：page=0 的预期（PARAM_ERROR）文档未定义该边界，按实际行为记录后反馈（你们之前也问过越界页，注意 0 和"超范围"是两码事）；
- **E-shop_069 等负数数量**：返回的是 **400**（业务校验），不是 422（格式校验），断言 HTTP 状态码时注意区分。

🟢 建议补充（缺口提示）

- 支付回调的**重复调用**场景（文档 3.15 明确写了"按幂等语义重复调用验证"，当前用例只覆盖单次成功/失败）；
- 下单成功后**购物车剩余条目**的核对断言（部分结算用例 076 只验了订单，没验购物车）；
- health 失败用例要停 MySQL，注意它会影响前后其他用例，建议放最后或独立执行。

------

详细评审意见我已落成文档：`docs/E-shop_单接口功能测试用例_评审意见.md`，每条问题带用例编号、级别、当前预期、正确预期、修改建议，你们改完用例后我可以复审。

Writetest_internship_simulation\project\docs\E-shop_单接口功能测试用例_评审意见.md

评审意见已落盘：**`docs/E-shop_单接口功能测试用例_评审意见.md`**（含问题清单、修改建议、复审流程），上面的回复就是摘要。

**结论一句话：不通过，修订后复审。** 重点三件事：

1. **B-1 端口（必须改）**：用例 URL 全部是 8001~8015，实际服务在 8000——不改的话一条都跑不起来；
2. **S-1~S-8（建议全改）**：`pagesize`→`page_size`、Query 参数类型错误预期、status 无枚举校验、库存不足错误码、确认收货文案/逻辑、占位符路径、404 vs 422 区分——这些是"预期和系统实际行为对不上"，不修的话执行时会大面积误报；
3. **S-9（不用改，但建议标注）**：已支付/已发货订单取消的用例预期（INVALID_STATE）是按状态机正确行为写的，当前实现确实不符——这个差异我们已登记缺陷单（接口文档 3.12 有记录），**建议保留预期并关联缺陷单号**，等修复后这条用例就是现成的回归验证。

另外提醒一句执行层面的：种子订单和秒杀库存是共享资源，多条用例互相消耗，建议要么每用例自造数据，要么明确执行顺序，否则用例之间会互相"误伤"——这个在 G-10 里写详细了。

你们修订完发我复审。

### 复审(2026-08-26)

未修订的 8 处（按新编号）

| 新编号             | 问题                                                         | 改法                        |
| ------------------ | ------------------------------------------------------------ | --------------------------- |
| E-shop_045/048/090 | keyword/category/status 传 `1` 预期 422——**Query 参数无类型校验**，实际 200 空列表 | 预期改 200                  |
| E-shop_089         | 标题"离点 0"但参数还是 999；且 page_size=0 实际返回 200 空列表 | 参数改 0、预期改 200 空列表 |
| E-shop_040/042     | page_size 是**请求值回显**：传 12 回显 12、传 1 回显 1，预期都写的 10 | 修回显值                    |
| E-shop_065         | 空路径 `/cart/items/` → 路由不匹配 **404**，不是 422         | 预期改 404 或删             |
| E-shop_067         | message 写「商品不存在」，实际是「购物车条目不存在」         | 修文案                      |
| E-shop_091         | `status = PENDING_PAYMENT"` 尾部**多一个引号**               | 删引号                      |
| E-shop_023/024     | 预期 nickname 写成字面量 `"username"`，实际是 test_14/test_15 | 修预期值                    |
| E-shop_052         | 未指定用户就预期 items 为空，alice 有 3 条种子               | 用 bob/carol 或改预期       |

顺带发现 3 处新问题

- **E-shop_005**：注册重名用 `dave`——dave 不是种子用户，**不存在时会注册成功**而不是 USER_EXISTS，建议改用 alice 或前置注明先注册 dave；
- **E-shop_141/142**：两条"订单 id 不存在"内容完全重复，删一条；
- **E-shop_079~081**：`[x]` 占位符的前置没写明"先加购 x 商品"（081 库存不足用例尤其需要先加购保温杯）。

我的建议

这 8 处都是**改预期值/参数值**的小改动，不涉及新增场景。如果你们想先跑起来，可以**先执行不涉及这 8 处的用例**（认证、商品、秒杀参数类），残留项改完再补跑——但丑话说前面：不修的话对应用例会**误记失败**，污染你们的通过率统计，回头还得返工。

改完再发我，这轮改完就真能进执行了。

### 复审(2026-08-26)

你的方法论立场完全正确，我正式采纳并修正了此前的评审思路——**预期代表规格，实现不符就是缺陷候选，测试不该迁就实现**。复审记录已更新（评审意见文档第十章），全部残留意见重新分类完毕：

分类结果

**① 缺陷候选（保留预期，执行后直接提 Bug 单）**：

| 用例           | 预期（规格）                      | 当前实现            |
| -------------- | --------------------------------- | ------------------- |
| E-shop_086     | page=0 → 参数错误                 | 500                 |
| E-shop_089     | page_size=0 → 参数错误            | 200 空列表          |
| E-shop_106/107 | 已支付/已发货取消 → INVALID_STATE | 200（已登记缺陷单） |

这三类执行后 FAIL 就是**有效的缺陷发现**，按 Bug 单提交，我们登记处理（修复排期按版本计划）。

**② 必须改（3 处，与实现无关的用例笔误，改了才能保证结果可信）**：

- **E-shop_042**：标题"上点 11"但参数 `page_size=1`——内部矛盾，响应回显必然等于请求值，二选一改齐（参数改 11 或预期改 1）；
- **E-shop_067**：文案「商品不存在」是从加购接口复制的，按标题语义应为「购物车条目不存在」；
- **E-shop_005**：`dave` 不是种子用户，不存在时用例会注册成功而非 USER_EXISTS——改用 alice。

**③ 前提不成立（建议删除或改成功预期）**：E-shop_045/048/090（keyword/category/status 传 `true`）——HTTP Query 参数**只能是字符串**，这条用例不可能命中任何缺陷，执行必 FAIL；报上来的话我们会按"设计如此"驳回（这不是缺陷，是协议特性），所以别浪费执行时间，删掉或改成 200 的成功预期。

执行约定（最终版）

1. ②③ 处理完即可执行；其余用例**原样执行**；
2. FAIL 即提 Bug 单（附请求/响应证据），我们对每个单给出"缺陷 / 设计如此 / 无法复现"的结论和依据——**驳回也会给理由，不是赖账**；
3. 已知差异（取消状态机）报单时关联缺陷单号。

评审到此结束，用例可以进执行了。执行中有任何"预期 vs 行为"的疑问，随时带证据提单。

## 搭建测试自动化框架(2026-08-26——08-27)

覆盖用例:E-shop_001 - E-shop_054,E-shop_056 - E-shop_075,E-shop_080 - E-shop_0151

## 冒烟测试(2026-08-27)



## 功能测试(2026-08-27——2026-08-28)

### 单接口功能测试:

产物:E-shop-Bug.html、Allure测试报告(index.html)

发现5个模块的9个缺陷

## 缺陷回归

### 开发验证:

#### Bug #4 取消已支付/已发货订单 —— ✅ 承认（严重度 1 接受）

**确认是缺陷**：取消接口的状态校验只拦了"已完成"，已支付、已发货订单都能被取消，与需求文档状态机（仅待支付可取消）不符。这条我们此前已登记（接口文档 3.12 有记录，之前澄清会也提过）。**修复排期 v1.1**，修复方案：仅待支付可取消，其余状态返回 400 `INVALID_STATE`；并与澄清会 #1（取消后库存回滚）联动，取消成功同时回滚库存、写库存流水——两个问题一套逻辑，避免改两遍。v1.0 冻结期不热修。

#### Bug #1 登录用户名不区分大小写 —— ❌ 驳回（设计如此）

`Alice` 能登录 `alice` 账号，根因是 MySQL 排序规则（utf8mb4_unicode_ci）不区分大小写。但注意：**注册侧同样不区分**（注册 `Alice` 会提示"用户名 alice 已被占用"）——全链路行为自洽，是常见的用户名策略，需求文档也未定义大小写敏感要求（你们用例里这条也标了"待确认"）。如需改为大小写敏感，涉及排序规则和注册/登录双重校验，属需求变更，排后续版本评估。

#### Bug #2 / #3 分页与类型校验 —— 部分承认 + 部分驳回

**承认（合并为一个缺陷，v1.1 修复）**：

- `page=0` → **500 INTERNAL_ERROR**：参数非法落到服务器内部错误，这个确实不该，应返回 422 参数错误（这条比较严重，优先修）；
- `page_size=0` → 200 空列表：分页参数应拒绝 0。

修复方案：page/page_size 增加 `ge=1` 校验（page_size 同时限 `le=100`），非法值统一 422 PARAM_ERROR。

**驳回（3 处"类型检验"）**：

- `keyword=1` / `category=1` / `status=1` 返回 200 而非 422——HTTP GET 的 Query 参数**本质就是字符串**，不存在"类型错误"这个概念。你们自己的结果里有个好证据：`keyword=1` 匹配到了商品"**USB-C 快充线 1m**"（total=1）——这正是字符串模糊搜索的正确行为。`status` 未做枚举校验属有意简化，如需枚举校验可排后续增强，不算缺陷。

#### Bug #5 创建订单请求体报 422 —— ❌ 驳回（无法复现）

我按文档场景实测了（8001 实例，6 种请求体）：

| 请求体                        | 结果                                                         |
| ----------------------------- | ------------------------------------------------------------ |
| **空 body（0 字节）**         | ✅ **不报 422**，正常走"结算全部"（购物车空则 CART_EMPTY）——文档 3.9"整个请求体可省略"成立 |
| 仅空格 / 单个 `{` / 残缺 JSON | 422 json_invalid（与你们报错同类）                           |

你们收到的 `json_invalid at ["body",8]` 只可能来自**非空且非法的请求体**——也就是说你们 JMeter 实际发出去的 Body Data 不是合法 JSON（比如引号被转义、尾逗号、或者 JSON 写进了 Parameters 标签页）。请把 JMeter 里 Body Data 的**原始内容**发我核对；如果确认是服务端问题，重新激活单子，我马上跟进。

------

**汇总**：驳回 2 整单（#1、#5）+ 2 单部分驳回（类型检验部分）；承认 2 个缺陷点（分页参数校验、取消状态机），**排 v1.1 统一修复**。另外提醒：你们 8000 端口的服务还是旧代码，之前修的错误响应结构（B2）需要重启服务才生效，重启后错误场景的断言才能按文档跑通。

### 开发修复:

E-Shop v1.1 发布说明

v1.1 已完成开发并全量验证，可以开始回归测试了。

#### 一、缺陷修复（3 个，对应你们提交的 Bug 单）

| 缺陷                                 | 修复内容                                                     | 实测验证                                               |
| ------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------ |
| **D1 分页校验**（Bug #2/#3）         | `page` ≥1、`page_size` 1~100，非法值返回 422 PARAM_ERROR（不再 500） | `page=0/-1`、`page_size=0/101` → 422 ✓                 |
| **D2 金额精度**                      | 订单/购物车金额改 Decimal 精确计算，消除浮点尾差             | 3×9.90 → 下单/详情/购物车均为精确 29.7 ✓               |
| **D4 取消状态机**（Bug #4 严重度 1） | 仅待支付可取消（其余 400 INVALID_STATE）；**取消成功回滚库存并写入库流水**（澄清会 #1 落地）；重复取消幂等 | 已支付取消 → 400；待支付取消 → 库存回滚+1 且流水新增 ✓ |

#### 二、故意保留（v1.2 修复，回归时仍可复现）

- **D3 越权**（bob 仍可查看/操作 alice 订单）、**D5 并发超卖**、**D6 支付回调无幂等/校验**——这三个 v1.1 **未修复**，回归时若复现属预期（sealed_defects.md 第五章已标注 v1.1 状态，供对照）。

#### 三、新增模块（8 个接口，总计 23 个业务接口）

- **收货地址**：列表/新增/修改/删除/设默认 5 个接口；默认地址规则（首个自动默认、设默认互斥、删默认自动补设）；**归属校验做了（非本人 403）**，这次没有重蹈 D3 覆辙；
- **订单评价**：提交/商品评价列表/订单评价列表 3 个接口；仅本人已完成订单可评价、单明细限评一次（重复 400 `ALREADY_REVIEWED`）；订单明细响应新增 `id` 字段作为评价入参（发现原设计缺这个字段，已补）。
- 预置数据：alice 已完成订单带 1 条种子评价；地址无预置。

#### 四、交付状态

- **主库已平滑升级**：新增 `addresses`、`reviews` 两张表，你们已有的测试数据全部保留；`start.bat` 启动会自动建新表，干净回归用 `start.bat --reset`；
- **文档全量更新**：README（v1.1）、需求文档（新模块 + 澄清会 #1 已实现）、接口文档（3.17 地址 / 3.18 评价 + 变更历史 + 错误码 403/ALREADY_REVIEWED）、已知问题清单（K-08/K-09）、openapi.json（v1.1.0，24 路径）；
- **验证方式**：全部改动在独立临时库实测通过后已清理，未污染测试环境。

#### 五、回归建议（测试策略由你们定，我只提示重点）

1. **修复项**：D1 边界（page=0 等）→ 422；金额精度（3×9.90 下单）；取消状态机（已支付/已发货取消 → 400，取消后库存恢复、流水有 ORDER_CANCEL 记录）；
2. **保留项**：D3/D5/D6 按 sealed 清单对照，仍可复现属预期；
3. **新模块**：地址归属（bob 操作 alice 地址 → 403）、评价规则（未完成订单、重复评价、他人订单）；
4. **服务重启**：8000 端口记得用新代码重启（Ctrl+C → start.bat）。

有回归问题随时提单。

### 测试重测:

测试通过

产物:Allure报告

# 问题

## 测试用例设计问题

### 1

```
接口测试，get请求传递参数，因URL 查询参数本质都是字符串，参数类型要求为string的种类，不写为string类型也可通过校验（例要求：status = "a"，但写为：status = a），如何设计才能实现类型校验？
```

你提出的这个问题，其实触及了 HTTP 协议和接口测试中一个非常经典的**概念误区**。我先给你一个最直接的结论：

**URL 查询参数（Query String）中，永远、也不需要加引号。** 
如果你在 GET 请求里写成 `status = "a"`（带双引号），服务器收到的实际值其实是 **`"a"`**（包含引号字符），这反而会导致业务逻辑判断失败，因为数据库里存的可能是 `a` 而不是 `"a"`。

针对你关心的“如何设计实现类型校验”，我们要把视角从“URL 语法”切换到“**后端业务逻辑校验**”。下面是两种场景下的正确设计方案和测试策略：

#### 1. 为什么 URL 本身无法做类型校验？
URL 参数在传输层（HTTP协议）中只有一种形态：**字节流（字符串）**。无论你传的是 `a`、`1` 还是 `true`，后端接收到的都是字符串。因此，**“类型校验”本质上是后端代码或框架层的责任**，而不是 URL 格式的责任。

#### 2. 后端到底是如何实现“类型校验”的？（开发者视角）
真正的类型校验在**后端 Controller（控制器）** 层实现，主要有三种方式：

- **强类型语言自动转换（如 Java Spring Boot）**：
  如果后端定义接口参数为 `Integer status`，但你传了字符串 `a`，框架会在进入方法前直接抛出 `TypeMismatchException`（状态码 400），这叫做**“类型绑定校验”**。
- **注解式校验（Bean Validation）**：
  即使参数是 `String`，后端可以加上 `@Pattern(regexp = "^[a-zA-Z]+$")` 或 `@Length(min=1, max=10)` 来限制内容格式。
- **枚举限制（最符合你的场景）**：
  如果业务要求 `status` 只能是 `a` 或 `b`，后端会把参数映射为 Java 的 `Enum` 类型。此时传 `c` 或 `1` 都会报错，而不会去纠结有没有引号。

#### 3. 作为测试人员，你的“设计”应该如何展开？
既然 URL 加不加引号都不影响类型，你应该**放弃“加引号”这个测试点**，转而设计以下更有效、能真正发现 Bug 的测试用例：

| 测试用例设计维度       | 针对参数的输入值（示例）                     | 预期后端结果                                                 | 测试目的                                    |
| :--------------------- | :------------------------------------------- | :----------------------------------------------------------- | :------------------------------------------ |
| **类型错误（异常值）** | 要求 String，传 数值 `123` 或 布尔 `true`    | **若后端未做强校验**，可能存入 `"123"`；**若加了正则**，则返回 400。 | 验证后端是否有参数校验注解（如 `@Pattern`） |
| **枚举边界值**         | 要求仅支持 `"a"` 和 `"b"`，传 `"c"` 或 `"A"` | 返回 400 并提示 `Invalid status value`                       | 验证枚举匹配逻辑是否区分大小写              |
| **超长字符串**         | 传 1000 个字符 `"a"`                         | 返回 413 或 400，数据库不应被撑爆                            | 验证 `@Length` 长度限制                     |
| **SQL/特殊字符**       | 传 `"a' OR '1'='1"`                          | 系统应正常转义，不报数据库异常                               | 验证预编译防注入                            |

#### 4. 给你一个直接的操作建议（针对接口测试）
- **不要尝试在 URL 参数里加引号**：直接在 JMeter 或 Postman 的参数栏里写 `a` 就是标准写法。
- **重点检查 Swagger/API 文档中的 `type` 和 `enum` 字段**：如果文档说 `type: string, enum: ["a", "b"]`，你的测试重点就应该是“传 `c` 报什么错”，而不是“传 `a` 通不通过”。
- **如果发现后端不校验类型**（传 `1` 也能成功存入字符串 `"1"`），你应该**提单（提Bug）**：建议后端开发加 `@Pattern` 或枚举校验，因为这是接口健壮性的缺失。

总结：**URL 参数没有“加引号”这个操作选项**。想要真正实现类型校验，靠的是后端代码里的校验注解，而你作为测试者，应该设计“**类型错误、枚举越界、特殊字符**”等测试数据来驱动开发完善这些校验逻辑。😊
</answer>

## 自动化断言设计

### 正确做法：逐字段断言

```
#断言：把预期结果解析为字典，逐个字段和响应JSON对比（响应多出的字段不影响断言）
expected = json.loads(case["预期结果"])
actual = response.json()
for key, value in expected.items():
    assert actual.get(key) == value, f"字段[{key}]断言失败：预期{value}，实际{actual.get(key)}，完整响应：{response.text}"
```

## "包含式"断言

```
for k, v in json.loads(case["预期结果"]).items():
    kv = f'"{k}":{json.dumps(v, ensure_ascii=False)}'
    assert kv in response.text, f"响应中未找到 {kv}，实际响应：{response.text}"
```

## 不了解的技术

### JWT鉴权

**JWT（JSON Web Token）** 是一种**“无状态”**的身份认证协议。你可以把它理解为一张**“加密的智能通行证”**。

在传统的 Session（会话）认证中，服务器需要存一份你的登录记录（占用服务器内存），而 JWT 则把这份记录**加密后存放在客户端**。服务器只需要持有“秘钥”，通过算法验证这张通行证是否被篡改即可，完全不需存储，因此特别适合分布式系统和微服务架构。

---

🔍 JWT 长什么样？（三部分构成）

一个 JWT 通常长这样（用 `.` 分隔的三段）：
`xxxxx.yyyyy.zzzzz`

| 部分          | 名称 | 作用                                                         | 举例                                            |
| :------------ | :--- | :----------------------------------------------------------- | :---------------------------------------------- |
| **Header**    | 头部 | 声明签名算法（如 HS256）和令牌类型                           | `{"alg":"HS256","typ":"JWT"}`                   |
| **Payload**   | 载荷 | **存放用户信息**（如 `user_id`、`username`）和过期时间（exp），**不建议放敏感信息**（如密码），因为这部分只是 Base64 编码，不是加密。 | `{"sub":"123","name":"Alice","exp":1690000000}` |
| **Signature** | 签名 | **防篡改核心**。由前两部分 + 服务器秘钥（Secret）通过指定算法生成。如果前两部分被篡改，签名验证会直接失败。 | `SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c`   |

---

⚙️ JWT 的工作流程（三步走）

1.  **登录（颁发）**：客户端（如 APP/浏览器）发送用户名密码。服务器验证通过后，创建一个包含用户 ID 的 JWT，返回给客户端。
2.  **存储**：客户端将 JWT 保存在本地（通常是浏览器的 `localStorage`、`SessionStorage` 或 Cookie）。
3.  **鉴权（请求）**：客户端在后续请求的 **HTTP Header（请求头）** 中携带 JWT，通常是：
    ```
    Authorization: Bearer <你的JWT字符串>
    ```
    服务器收到请求后，用秘钥验证 JWT 的签名是否正确、是否过期。验证通过则处理请求，否则返回 `401 Unauthorized`（未授权）。

---

📊 JWT vs 传统 Session（为什么要用它？）

| 维度          | 传统 Session（服务端存储）                        | JWT（客户端存储）                                            |
| :------------ | :------------------------------------------------ | :----------------------------------------------------------- |
| **存储位置**  | 服务器内存 或 Redis（占用资源）                   | 客户端（不占用服务器存储）                                   |
| **扩展性**    | 需要配置共享存储（如 Redis 共享 Session），复杂。 | **天然无状态**，任意服务器节点都可验证，水平扩展极方便。     |
| **跨域/跨端** | 依赖浏览器 Cookie，移动端（APP）支持繁琐。        | 在 Header 中传递，**完美适配 APP、小程序、前后端分离跨域场景**。 |
| **注销控制**  | 服务器直接删除 Session，立即失效。                | **无法主动失效**（只能等过期），这是 JWT 最大的痛点。        |

---

⚠️ 必知的安全铁律（测试/开发必看）

- **绝对不要存敏感信息**：Payload 只是 Base64 编码（相当于明文传输），任何人都能解码看到内容。**严禁存放密码、银行卡号**。
- **必须使用 HTTPS**：防止 JWT 在传输过程中被“中间人”截获（劫持）。
- **设置短期过期时间 + Refresh Token（刷新令牌）**：JWT 一旦发出无法撤回，必须设置较短有效期（如 15分钟-2小时），避免因令牌泄露导致永久权限丢失。

---

🛠️ 在 JMeter 中如何传递 JWT？

在你之前的接口测试脚本中，通常在 HTTP 请求下添加一个 **HTTP 信息头管理器（HTTP Header Manager）**，添加一行：
- **名称**：`Authorization`
- **值**：`Bearer ${your_jwt_token_variable}`

（将提取到的 Token 存入变量，供后续所有需要鉴权的接口使用）😊

## Http请求失败

请求方式错误

# 补充点

1. 

   1. **原子性**：如果列表里有无效条目或不属于当前用户的条目 ID，整个请求返回 400 `PARAM_ERROR`，**不会**出现"部分条目成功下单"的中间态；库存不足同理，整单失败（400 `INSUFFICIENT_STOCK`），购物车不变。
   2. 这个"部分结算"只在 `POST /orders` 生效，`POST /seckill/orders` 是按单个商品直接下单，不走购物车，没有这个参数。

2. 已更新完毕，`docs/接口文档.md` 现在**每个接口都有 ✅ 成功案例 + ❌ 失败案例**（16 个路径全含），而且每个失败案例我都实际请求验证过，响应体和状态码与文档逐字一致。

   **本轮改动**：

   1. 通用约定新增"案例标注"说明（✅/❌ 的含义）；
   2. 逐接口补失败案例，覆盖四类典型失败：
      - **鉴权失败**：未带 token / token 无效（401 UNAUTHORIZED，两种文案不同——未登录 vs 登录状态无效，注意区分）；
      - **资源不存在**：商品/订单/购物车条目不存在（404 NOT_FOUND）；
      - **参数校验失败**：类型错（`page=abc`）、长度不足（密码 3 位）、数量越界（quantity=0 → 422，details 原样给出）；
      - **业务规则失败**：用户名重复（400 USER_EXISTS）、密码错误（401 INVALID_CREDENTIALS）、购物车为空（400 CART_EMPTY）、条目非本人（400 PARAM_ERROR）、状态机非法流转（400 INVALID_STATE，三个接口各一例）、非秒杀商品（400 NOT_SECKILL）。

   **几个写文档时确认到的行为细节**（你们断言时可以直接用）：

   - 取消/发货/确认收货的 INVALID_STATE 文案各不相同（"已完成订单不可取消"/"仅已支付订单可发货"/"仅已发货订单可确认收货"），别在断言里写成一个；
   - 改数量接口的校验顺序：条目不存在优先返回 404，条目存在时才校验负数 → 400，所以"负数数量"用例要建立在条目存在的前提下；
   - health 接口没有 4xx/5xx 路径，失败案例给的是数据库不可达时的降级响应（200 + `degraded`），它不是 HTTP 错误，注意断言写法；
   - 422 的 `details` 结构给了两个完整真实样例（`string_too_short` 和 `greater_than_equal`），其余 422 结构相同。

   如果你还要"响应字段级"的完整字段表（每个响应字段的类型/可空），`docs/openapi.json` 里有机器可读版本，需要的话我再出一版 Markdown 附录。

# 参考流程

基于目前主流的**敏捷 + DevOps** 模式，一份详细且可落地的测试流程通常分为 **5 个核心阶段**。这套流程能覆盖需求分析到上线监控的全生命周期。

------

### 阶段一：需求评审与测试计划（介入越早越好）

- **核心动作**：
  - 参加需求评审会（PRD评审），站在测试视角提出**可测性问题**（如“这个‘高并发’具体是多少QPS？”、“异常状态码未定义”）。
  - 进行**测试工作量评估**，输出《测试计划》，明确测试范围、资源（人/环境）、排期、风险预案。
  - 确定**测试策略**：哪些模块做全量回归（核心交易链路）、哪些做自动化回归、哪些必须做安全渗透。
- **关键产出**：《测试计划》、《风险矩阵表》。

------

### 阶段二：测试用例设计（右移 & 左移结合）

这是最体现工程能力的阶段，分为**手工用例**和**自动化用例**两条线：

- **手工用例设计（核心基线）**：
  - 针对**正常场景**：使用**等价类划分 + 边界值分析**覆盖输入域。
  - 针对**异常场景**：使用**决策表/因果图**梳理复杂业务规则（如优惠券叠加、退款状态流转）。
  - 针对**状态机**：绘制订单/工单状态转换图，确保每个跳转路径都被覆盖。
  - 导出《测试用例（Excel/TestLink）》，并组织 **用例评审**。
- **自动化用例设计（左移储备）**：
  - **接口层**：根据接口文档（Swagger/YApi）设计 **Pytest + Requests** 框架的自动化脚本结构。
  - **单元测试层**：要求开发编写 **JUnit/Pytest** 单元测试，测试人员抽查覆盖率是否达标（通常要求 >= 80%）。
- **关键产出**：《测试用例文档》、《接口自动化脚本框架》、《单元测试覆盖率报告》。

------

### 阶段三：环境部署与冒烟测试（提测闸门）

这是开发将代码移交测试的**准入关口**，**只有冒烟通过，才正式进入系统测试**：

- **环境准备**：维护稳定的测试环境（Test环境），数据库、中间件、外部依赖（如支付沙箱）需配置完备。**强烈建议使用 Docker Compose 一键拉起环境**。
- **冒烟测试（BVT）**：
  - 执行核心主流程用例（如：注册 → 登录 → 下单 → 支付 → 回调），**由开发自测通过后发起“提测申请单”**。
  - 若冒烟失败（如首页报500），**测试有权打回提测**，并记录质量红线（如连续打回2次，影响开发绩效）。
- **关键产出**：《冒烟测试报告》、《提测环境配置手册》。

------

### 阶段四：系统测试与缺陷管理（核心执行期）

这是测试人员投入精力最大的阶段：

- **功能测试（ST）**：
  - 按照《测试用例》逐条执行，发现Bug后录入缺陷管理工具（如 Jira、TAPD）。
  - 遵循 **Bug生命周期**：新建 → 开发修复 → 测试验证 → 回归关闭。
- **专项测试（深水区）**：
  - **接口自动化回归**：每天定时（如凌晨）运行自动化脚本，产出 Allure 报告，及时发现代码合入引入的回归问题。
  - **性能测试**：针对核心接口（如秒杀下单），用 **JMeter** 模拟高并发（如 1000 TPS），监控响应时间、CPU/内存、数据库连接池。
  - **安全测试（DevSecOps）**：使用 OWASP ZAP 或 SQLMap 进行**渗透测试**，验证是否存在 SQL 注入、XSS 攻击、越权访问。
- **关键产出**：《缺陷统计表》、《性能测试报告》、《接口自动化回归日报》。

------

### 阶段五：验收测试与上线

- **UAT（用户验收测试）**：
  - 产品经理或客户代表参与，验证业务流程是否符合预期。
  - 输出 **《验收测试确认书》**，作为上线的业务准入凭证。
- **预发布环境验证（Pre-release）**：
  - 在生产环境的镜像环境下（数据库与生产隔离）执行**最终回归测试**。
  - 验证数据库脚本、配置项（如Nginx转发规则）是否正确。
- **上线与冒烟监控**：
  - 发布后，实时查看 **APM（应用性能监控，如 SkyWalking）** 和日志平台（ELK），关注错误率（Error Rate）是否突然飙升。
  - 上线后 **24小时内** 安排值班人员，快速响应用户反馈。
- **关键产出**：《上线报告》、《线上监控大盘配置》。

------

### 💡 现代测试流程的 3 个核心理念（避坑指南）

1. **测试左移**：**不要在提测后才开始测试**。代码Review阶段就要介入，单元测试阶段就要开始Check覆盖率。
2. **持续测试**：**不要等版本发布前才跑全量用例**。每次代码合入合并请求（MR）都触发 Pipeline 自动跑核心用例。
3. **质量内建**：测试不只是“找Bug”，而是通过流程卡点（如冒烟、SonarQube质量门禁）**倒逼开发提升代码质量**。

掌握以上流程，无论你是功能测试、自动化还是测试开发岗，都能完整把控项目质量全景。😊



# 模板

## 测试计划

这是一个标准的**敏捷开发（Agile）一周迭代**测试计划示例。它假设开发在**周一/周二**完成主要功能开发，**周三**提测，**周四**完成测试，**周五**上线。这个计划强调“时间盒”和“质量卡点”。

---

📅 项目名称：E-Shop 用户中心 V2.3.0 迭代测试计划

### 一、迭代基本信息

- **迭代周期**：2026-08-24（周一）至 2026-08-28（周五）
- **测试版本**：E-Shop_UserCenter_V2.3.0
- **测试负责人**：向俊坤
- **测试环境**：Test 环境（MySQL 8.0 + Redis 6.2 + 微服务 5 个节点）
- **核心需求**：
  - 新增“手机号一键注册”功能（P0）
  - 修改“密码加密算法”为 SHA-256 + 盐（P0）
  - 修复“订单列表分页偏移量错误”Bug（P1）
  - 优化“登录接口响应时间”至 200ms 以下（P1）

---

### 二、测试资源与角色分工

| 角色       | 姓名   | 职责                               |
| :--------- | :----- | :--------------------------------- |
| 测试工程师 | 向俊坤 | 负责 80% 功能测试 + 自动化脚本维护 |
| 开发工程师 | 张工   | 修复 Bug，配合排查环境问题         |
| 产品经理   | 李经理 | 参与 UAT 验收（周五下午）          |

---

### 三、迭代日历（按天拆解）

#### 【周一】需求评审 & 用例设计（左移）

- **上午（10:00 - 11:00）**：参加迭代计划会，确认需求范围和优先级。
- **下午（13:30 - 17:00）**：
  - 编写 **《测试用例》**（含正常/异常/边界场景，约 60 条）。
  - **自动用例准备**：在接口自动化框架（Pytest + Requests）中创建新接口的测试脚本骨架。
  - **性能脚本准备**：用 JMeter 搭建登录接口压测脚本（含 CSV 参数化）。

- **产出物**：
  - 《V2.3.0 测试用例.xlsx》
  - 自动化脚本 PR（提交代码仓库）

---

#### 【周二】用例评审 & 自动化脚本开发

- **上午（09:30 - 10:30）**：组织用例评审会（开发 + 产品参会），修正 5 个歧义点。
- **上午（10:30 - 12:00）**：执行 **冒烟测试用例预跑**（使用开发自测环境），提前发现环境问题。
- **下午（14:00 - 18:00）**：
  - 完善自动化脚本（含数据清理 + 断言）。
  - 配置 JMeter 压测参数（梯度压测：10/50/100 并发）。

- **风险预警**：开发冒烟未通过 → 推迟提测至周三晚。

---

#### 【周三】功能测试（第一轮全量执行）

- **全天（09:30 - 18:00）**：
  - **冒烟测试（准入闸门）**：执行核心主流程（注册 → 登录 → 下单 → 支付），**全部通过后才接受版本**。
  - **第一轮全量测试**：按用例逐条执行（手工），发现 Bug 录入 TAPD。
  - **自动化回归**：触发 Pipeline 执行已有接口自动化（确保新功能未破坏老功能）。

- **关键节点**：
  - **下班前（17:30）**：输出 **《第一轮测试报告》**，列出阻塞性问题清单。
  - 如果当天发现 **P0 级 Bug（必现闪退/数据错乱）**，要求开发当晚必须修复，否则版本打回。

---

#### 【周四】回归测试 & 性能测试

- **上午（09:30 - 12:00）**：
  - 验证开发修复的 Bug（逐个验证 + 回归关联模块）。
  - 执行 **第二轮全量回归**（针对修改影响范围）。
- **下午（13:30 - 17:30）**：
  - **性能测试**：用 JMeter 压测登录接口（目标 QPS ≥ 500），收集 `聚合报告` 和 `响应时间分布图`。
  - **安全测试**：使用 OWASP ZAP 对注册/登录接口进行自动化扫描，验证 SQL 注入防护。
- **下班前（17:30）**：输出 **《性能测试报告》** 和 **《安全扫描报告》**。

---

#### 【周五】UAT 验收 & 上线

- **上午（09:30 - 11:30）**：
  - **UAT（用户验收测试）**：产品经理在预发布环境执行业务流程验收（3 个核心场景）。
  - **上线检查清单确认**：
    - [ ] 数据库脚本已执行（含回滚方案）
    - [ ] Nginx/网关配置已更新
    - [ ] 监控大盘（Prometheus + Grafana）已配置告警
- **下午（14:00 - 15:00）**：
  - **生产发布**（灰度发布：先 5% 流量验证）。
  - **线上冒烟测试**：在新生产环境执行 10 个核心接口的自动化脚本。
- **下午（15:00 - 17:00）**：
  - 观察线上日志（ELK），关注 `ERROR` 级别日志量。
  - 发布 **《V2.3.0 上线报告》**，总结本次迭代测试数据：
    - 总用例数 / 通过数 / 失败数
    - 发现 Bug 数（P0/P1/P2）
    - 自动化用例覆盖率和通过率

---

### 四、质量门禁（必须达标）

| 门禁项                | 标准                       | 责任方      |
| :-------------------- | :------------------------- | :---------- |
| 冒烟测试通过率        | **100%**（核心主流程）     | 开发 + 测试 |
| 严重 Bug（P0/P1）数量 | **0 个未修复**             | 开发        |
| 接口自动化覆盖率      | **≥ 70%**（核心接口）      | 测试        |
| 性能测试（登录接口）  | **QPS ≥ 500**，错误率 < 1% | 开发 + 运维 |
| 安全扫描              | **无高危漏洞**             | 开发        |

---

### 五、风险预案

| 风险场景                     | 应对措施                                                |
| :--------------------------- | :------------------------------------------------------ |
| 开发延期（周三未提测）       | 压缩测试时间：优先保证 P0 用例，P2 用例延后至下个迭代   |
| 测试环境不稳定（数据库宕机） | 改用 Docker 本地环境，或借用预发布环境                  |
| 性能测试不达标               | 记录瓶颈点（如慢 SQL、内存泄漏），下个迭代优先级提至 P0 |
| 上线后紧急回滚               | 准备回滚脚本（SQL + 代码版本），15 分钟内执行回滚       |

---

### 六、测试报告模板（最终交付物）

```
E-Shop V2.3.0 测试报告

1. 测试概况
   - 总用例数：62 条
   - 通过：58 条（93.5%）
   - 失败：4 条（均为 P2 界面优化问题，已记录，下个迭代修复）

2. Bug 统计
   - P0：0 个
   - P1：1 个（已修复并验证）
   - P2：5 个（已修复 3 个，剩余 2 个不影响上线）

3. 性能测试结果
   - 登录接口 TPS：562
   - 平均响应时间：145ms
   - 结论：达标

4. 风险说明
   - 手机号一键注册功能因短信通道延迟，需灰度验证 2 小时。

5. 结论：准予上线 ✅
```

这个计划既能确保质量，又能适应敏捷开发的快节奏。你可以根据自己项目的实际情况调整时间分配和测试深度。😊
</answer>

# 测试计划

---

📅 项目名称：E-Shop 用户中心 V1.0 迭代测试计划

### 一、迭代基本信息

- **迭代周期**：2026-08-25（周二）至 2026-08-28 (周五)
- **测试版本**：E-Shop v1.0
- **测试负责人**：向俊坤
- **测试环境**：
  - MySql 8.0.45、
  - Python 3.14.6、版本	
  - Windows 11 家庭中文版
    版本号	23H2
    安装日期	‎2025/‎1/‎10
    操作系统版本	22631.4751
    体验	Windows 功能体验包 1000.22700.1055.0
  - Jmeter 5.6.3
- **核心需求**：
  - 


---

### 二、测试资源与角色分工

| 角色       | 姓名   | 职责                                                         |
| :--------- | :----- | :----------------------------------------------------------- |
| 测试实习生 | 向俊坤 | 需求评审、用例设计、冒烟测试、功能测试、回归测试、性能测试、自动化框架搭建 |

---

### 三、迭代日历

#### 【周二】需求评审 & 用例设计（左移）

- **周二（8:00 - 12:00）**：参加迭代计划会，确认需求范围和优先级。
- **周二（14:30 - 17:00）**：
  - 编写 测试用例（含正常/异常/边界场景，约条）。
- **产出物**：
  - 《V1.0 测试用例.xlsx》

---

#### 【周二】用例评审 & 搭建自动化框架

- **晚上（09:30 - 10:30）**：
  - 组织用例评审会（开发 + 产品参会）。
  - 执行 **冒烟测试用例预跑**（使用开发自测环境），提前发现环境问题。
  - 

- **风险预警**：开发冒烟未通过 → 推迟提测至周三晚。

---

#### 【周三】功能测试（第一轮全量执行）

- **全天（09:30 - 18:00）**：
  - **冒烟测试（准入闸门）**：执行核心主流程（注册 → 登录 → 下单 → 支付），**全部通过后才接受版本**。
  - **第一轮全量测试**：按用例逐条执行（手工），发现 Bug 录入 TAPD。
  - **自动化回归**：触发 Pipeline 执行已有接口自动化（确保新功能未破坏老功能）。

- **关键节点**：
  - **下班前（17:30）**：输出 **《第一轮测试报告》**，列出阻塞性问题清单。
  - 如果当天发现 **P0 级 Bug（必现闪退/数据错乱）**，要求开发当晚必须修复，否则版本打回。

---

#### 【周四】回归测试 & 性能测试

- **上午（09:30 - 12:00）**：
  - 验证开发修复的 Bug（逐个验证 + 回归关联模块）。
  - 执行 **第二轮全量回归**（针对修改影响范围）。
- **下午（13:30 - 17:30）**：
  - **性能测试**：用 JMeter 压测登录接口（目标 QPS ≥ 500），收集 `聚合报告` 和 `响应时间分布图`。
  - **安全测试**：使用 OWASP ZAP 对注册/登录接口进行自动化扫描，验证 SQL 注入防护。
- **下班前（17:30）**：输出 **《性能测试报告》** 和 **《安全扫描报告》**。

---

#### 【周五】UAT 验收 & 上线

- **上午（09:30 - 11:30）**：
  - **UAT（用户验收测试）**：产品经理在预发布环境执行业务流程验收（3 个核心场景）。
  - **上线检查清单确认**：
    - [ ] 数据库脚本已执行（含回滚方案）
    - [ ] Nginx/网关配置已更新
    - [ ] 监控大盘（Prometheus + Grafana）已配置告警
- **下午（14:00 - 15:00）**：
  - **生产发布**（灰度发布：先 5% 流量验证）。
  - **线上冒烟测试**：在新生产环境执行 10 个核心接口的自动化脚本。
- **下午（15:00 - 17:00）**：
  - 观察线上日志（ELK），关注 `ERROR` 级别日志量。
  - 发布 **《V2.3.0 上线报告》**，总结本次迭代测试数据：
    - 总用例数 / 通过数 / 失败数
    - 发现 Bug 数（P0/P1/P2）
    - 自动化用例覆盖率和通过率

---

### 四、质量门禁（必须达标）

| 门禁项                | 标准                       | 责任方      |
| :-------------------- | :------------------------- | :---------- |
| 冒烟测试通过率        | **100%**（核心主流程）     | 开发 + 测试 |
| 严重 Bug（P0/P1）数量 | **0 个未修复**             | 开发        |
| 接口自动化覆盖率      | **≥ 70%**（核心接口）      | 测试        |
| 性能测试（登录接口）  | **QPS ≥ 500**，错误率 < 1% | 开发 + 运维 |
| 安全扫描              | **无高危漏洞**             | 开发        |

---

### 五、风险预案

| 风险场景                     | 应对措施                                                |
| :--------------------------- | :------------------------------------------------------ |
| 开发延期（周三未提测）       | 压缩测试时间：优先保证 P0 用例，P2 用例延后至下个迭代   |
| 测试环境不稳定（数据库宕机） | 改用 Docker 本地环境，或借用预发布环境                  |
| 性能测试不达标               | 记录瓶颈点（如慢 SQL、内存泄漏），下个迭代优先级提至 P0 |
| 上线后紧急回滚               | 准备回滚脚本（SQL + 代码版本），15 分钟内执行回滚       |

---

### 六、测试报告模板（最终交付物）

```
E-Shop V2.3.0 测试报告

1. 测试概况
   - 总用例数：62 条
   - 通过：58 条（93.5%）
   - 失败：4 条（均为 P2 界面优化问题，已记录，下个迭代修复）

2. Bug 统计
   - P0：0 个
   - P1：1 个（已修复并验证）
   - P2：5 个（已修复 3 个，剩余 2 个不影响上线）

3. 性能测试结果
   - 登录接口 TPS：562
   - 平均响应时间：145ms
   - 结论：达标

4. 风险说明
   - 手机号一键注册功能因短信通道延迟，需灰度验证 2 小时。

5. 结论：准予上线 ✅
```

这个计划既能确保质量，又能适应敏捷开发的快节奏。你可以根据自己项目的实际情况调整时间分配和测试深度。😊

# 需求澄清问题

## 接口文档

### 用户注册

```
nicknamestring否≤30 字符`大卫`昵称; 省略或空串时默认取 username
```

username指的是"username"还是{username}